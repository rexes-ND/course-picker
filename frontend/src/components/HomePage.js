import Header from './Header'
const HomePage = (props) => {
    return (
        <>
            <Header login={props.login}/>
            {/* <img src={logo}></img> */}
            HomePage
           
        </>
    )
}

export default HomePage